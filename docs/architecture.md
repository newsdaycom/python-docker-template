# Architecture

Describe the generated service's purpose, boundaries, runtime flow, and dependencies.

## Responsibilities

- Document what this service owns.
- Document what this service explicitly does not own.

## Runtime Flow

Add a concise request, job, or data-flow description once the service has real behavior.

## Dependencies

List required upstream services, downstream services, files, databases, queues, APIs, and scheduled jobs.
